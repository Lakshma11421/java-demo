name: Python CI

on:
  push:
    branches:
      - master  # Trigger on push to the master branch
  workflow_dispatch:  # Allow manual trigger via GitHub UI

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2  # Checks out the repository

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.9'

    - name: Install pip-tools
      run: |
        pip install --upgrade pip
        pip install pip-tools  # Install pip-tools

    - name: Compile requirements
      run: |
        # Use pip-compile to generate the requirements.txt with the correct, resolved versions
        pip-compile --output-file=requirements.txt ./requirements.txt  # Path to your original requirements file
        cat requirements.txt  # Debugging step to ensure correct requirements are generated

    - name: Install dependencies
      run: |
        pip install -r requirements.txt  # Install the generated dependencies
